import React from 'react'

interface Props {}

function PassengerCard(props: Props): React.JSX.Element {
    const {} = props

    return (
        <div>
            PassengerCard
        </div>
    )
}

export default PassengerCard
